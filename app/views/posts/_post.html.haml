%section{:id=>"post_#{post.id}"}
  .page-header
    %h1= post.title
    %p.date= post.created_at.strftime("%d.%m.%Y")

  = post.shorten.try(:html_safe) || post.body.html_safe
  .more_link
    %a{:href=>post_path(post)}= post.cut || "More"